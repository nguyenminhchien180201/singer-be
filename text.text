-node_modules nơi chứa thư viện javascript 
-body-parser viết API, giúp client lấy tham số gửi lên server 
-ejs chạy trên server 
-babel dịch theo chuẩn javascript để các phiên bản khác nhau có thể hoạt động chung 
-promise chờ xử lý xong mới chạy tiếp 
-resolve để thoát khỏi 1 promise  
-raw: true tránh lấy ra những thứ k cần thiết kiểm tra form chuẩn api restapi 
-api giao tiếp giữa server với server không trả ra html, chỉ trả về dữ liệu sạng json object 
-except file => get ra data chỉ lấy một vài trường 